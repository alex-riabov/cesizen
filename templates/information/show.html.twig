{% extends 'base.html.twig' %}

{% block title %}Information{% endblock %}

{% block body %}
    <h1>Information</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ information.id }}</td>
            </tr>
            <tr>
                <th>NomInformation</th>
                <td>{{ information.nomInformation }}</td>
            </tr>
            <tr>
                <th>DateHeureInformation</th>
                <td>{{ information.dateHeureInformation ? information.dateHeureInformation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>ContenuInformation</th>
                <td>{{ information.contenuInformation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_information_index') }}">back to list</a>

    <a href="{{ path('app_information_edit', {'id': information.id}) }}">edit</a>

    {{ include('information/_delete_form.html.twig') }}
{% endblock %}
