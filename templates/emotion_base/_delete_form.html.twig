<form method="post" action="{{ path('app_emotion_base_delete', {'id': emotion_base.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ emotion_base.id) }}">
    <button class="btn">Delete</button>
</form>
