{% extends 'base.html.twig' %}

{% block title %}EmotionBase{% endblock %}

{% block body %}
    <h1>EmotionBase</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ emotion_base.id }}</td>
            </tr>
            <tr>
                <th>NomEmotionBase</th>
                <td>{{ emotion_base.nomEmotionBase }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_emotion_base_index') }}">back to list</a>

    <a href="{{ path('app_emotion_base_edit', {'id': emotion_base.id}) }}">edit</a>

    {{ include('emotion_base/_delete_form.html.twig') }}
{% endblock %}
